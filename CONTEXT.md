# changemap

コードの変更とファイル間の関係を俯瞰し、レビュー中の理解を支える。

## Language

**変更マップ（Change map）**:
変更されたファイルと、それらに関連するファイルの依存関係を示す図。レビューする順番は利用者が選ぶ。
_Avoid_: レビュー順序表、影響範囲の保証

**変更状態（Change status）**:
比較する二つの状態の間で、ファイルが追加・変更・削除されたこと、または依存関係が追加・削除されたことを表す区分。ファイルの変更と依存関係の変更は別々に判定する。

**レビュー用スナップショット（Review snapshot）**:
一回の比較に対応する、差分・ファイル全文・依存関係・解析設定をまとめた固定済みの状態。利用者が明示的に更新するまで、レビュー中に参照する内容の基準となる。

**ファイル依存関係（File dependency）**:
import・再エクスポート・CommonJSのrequireなどによる、参照先ファイルを特定できるモジュール参照の関係で、型だけの参照も含む。同じ向きの同じファイル間の参照は一つの関係として扱い、実行時の呼び出しやデータの流れを意味しない。

**テーマ（Theme）**:
グラフ、コード表示、周辺のUIに適用する一貫した見た目。

**未解決の参照（Unresolved reference）**:
依存解析で参照先を特定できなかったモジュール参照。参照先が存在しないと断定するものではなく、意図的に表示対象から除外した参照とも区別する。

**解析対象外の変更ファイル（Unanalyzed changed file）**:
差分閲覧の対象だが、依存関係の解析対象には含まれない変更ファイル。依存関係が存在しないと確認されたファイルを意味しない。
