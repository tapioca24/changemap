# フェーズ5：品質と初版範囲

状態：実装・測定・CI検証完了（2026-09-23）。TASK-12はDone。PRのマージとnpm公開は未実施。

## 実装計画

- [x] 既存のCLI・Git・解析・グラフ・更新・設定・UIテストを確認し、不足する統合検証を追加する。
- [x] 実際のUIを用いる再現可能な大規模グラフ測定を追加し、配置・描画・操作の時間と限界を記録する。
- [x] 測定で判明した性能問題を修正し、回帰検証する。
- [x] 型チェック・lint・整形・全テスト・build・隔離配布検証を実施する。
- [x] 3 OS・Node.js最低版と24系のCI結果を確認する。今回の変更の結果と過去の結果を区別する。
- [x] 英語READMEの初版範囲・性能境界、Backlog、引き継ぎを更新する。
- [x] 変更をレビューできる状態にする。[PR #6](https://github.com/tapioca24/changemap/pull/6) を作成済み。difitの実施記録は確認できないため、レビュー導線はPRを記録する。

## 対象と制約

npm公開は別途指示が必要。実装コミット `4d09593` を `feat/phase-5-quality-release` にpushし、フェーズ4ブランチを比較元とするPR #6を作成済み。build依存を含むpackage.jsonとpnpm-lock.yamlの既存変更は保持されている。

## 合意済みの性能目標

表示対象100ノード・300辺で初期表示1秒以内、1,000ノード・3,000辺で3秒以内、選択・テーマ変更300ms以内をユーザー承認済み。10,000ノードは限界測定とする。リポジトリ全体の解析ファイル数と、直接近傍抽出後の表示ノード数を区別する。

## 調査記録

既存の87テストは各Git入力、変更状態、解析、rename両端の統合、更新失敗、設定保存失敗などを検証している。UIの統合状態遷移は手動検証記録が中心。CIにはubuntu/macOS/Windows × Node.js 24.11.0/24.xの6ジョブがある。

## 実装と検証範囲

- Appをエントリポイントから分離し、実際のAPIクライアント・コードペインを使って更新通知、失敗保持、再試行、ID再利用時の選択解除、空比較、画面外の解析不完全、設定保存失敗と4テーマ・4方向の状態遷移を自動検証した。jsdomではグラフ部品だけを代替し、配置とブラウザ描画は別途検証する。
- 配布済みCLIを起動する統合テストを全7入力パターンへ追加した。HEAD→index→worktreeの異なる内容と、変更前後の全文・統合辺が同じ比較を表すことを確認する。
- 既存のGit状態・依存抽出・rename統合・設定永続化テストを維持。全98テストがmacOSで成功。
- GraphとFileNodeをmemo化し、選択していないノードのデータとコールバックを保持。テーマ変更・定期ポーリングでグラフ全体を再描画しない。未解決参照はノードごとの全件走査から状態別Setへ変更した。
- 配置を独立モジュールへ分離。Dagreが例外を返す場合、ファイル選択でコード閲覧を継続する。参照情報を消した成功扱いにはしない。
- CIの重複build（testとpackがbuildする）を1,000ノードの配置検証に置換。ノード数・辺数・有限な座標と経路を確認し、CIマシンの速度では合否を判定しない。

## 測定方法と結果

`pnpm build`後、`pnpm benchmark:rendering <nodes> [layered|chain|hub] [screenshot-path]`を実行する。依存は既存のagent-browserとChromiumを利用し、実行時依存は追加しない。専用のloopback fixture serverが実際のdist/uiと合成ReviewSummaryを配信する。ブラウザの初期描画はheadの計測スクリプト開始から、全ノード・辺のDOM準備と2回のrequestAnimationFrameまで。選択・テーマ変更は各5回、同じく2フレーム待って測る。Git取得・解析、GPU描画完了、実ユーザーのINPは含まない。

100/1,000ノードは新しいブラウザセッションで各3回。環境はApple M5、Darwin 25.6.0、Node.js 24.14.1、headless Chromium 153、1440×1000。測定値は [phase-5-measurements.json](phase-5-measurements.json) に保存。測定中の他プロセスをOSレベルで隔離してはいない。

| 形状            | ノード / 辺   | 初期描画       | 選択最大 | テーマ変更最大 |
| --------------- | ------------- | -------------- | -------- | -------------- |
| 階層（3回）     | 100 / 300     | 136.4–145.7 ms | 34.3 ms  | 33.9 ms        |
| 階層（3回）     | 1,000 / 3,000 | 781.6–825.6 ms | 34.6 ms  | 42.4 ms        |
| 依存鎖（1回）   | 1,000 / 999   | 310.5 ms       | 33.5 ms  | 33.7 ms        |
| 集中依存（1回） | 1,000 / 999   | 367.2 ms       | 37.6 ms  | 34.0 ms        |

最適化前の1,000ノードでは初期740.6 ms、選択最大154.8 ms・テーマ最大152.2 ms。最適化の効果は主に表示後の操作にある。初期描画の改善とは主張しない。集中依存のスクリーンショットを確認し、全体表示ではラベルが小さいことを確認した。ズーム・パンが必要であり、1,000ノードすべてを一画面で読めるという保証ではない。

10,000ノード・30,000辺はDagreのスタック上限で失敗した。修正前はブラウザ測定が25秒でタイムアウト。単体配置測定で435 ms後の `RangeError: Maximum call stack size exceeded`、NodeピークRSS 322.6 MiBを確認した。例外処理追加後は10,000ファイルの選択肢を維持し、31 msでコードを開けた。グラフの描画成功には数えず、benchmarkも終了コード1を返す。

`pnpm benchmark:layout 1000` は最新実行で661 ms / Nodeピーク343 MiB。配置専用プロセスのRSSでありブラウザメモリではない。`pnpm benchmark:analysis 100 10000` は100ファイル37/13 ms・136 MiB、10,000ファイル655/589 ms・741 MiBだった。解析は既存の約143 bytes/ファイルのfixtureで、表示対象は6ファイル。描画1,000ノード測定と混同しない。

制限：同期配置が長時間かかるケースを中断する機能はない。Windows/Linuxのブラウザ描画、任意の密なグラフや大きい実リポジトリを保証する測定ではない。大規模対応の拡張（別配置方式、worker、集約）は初版の対応範囲へ追加していない。

## 品質・配布・CI

- 全98テスト、型チェック、lint、build、空のstore/cacheからの隔離配布検証が成功。整形チェックも対象コミットのCIで成功。
- 配布検証ではpnpm pack、クリーンな導入、bin、JS/CSS・ライセンス、固定全文、解析設定の更新、設定初回保存を確認した。
- 初回の追加テストにreadonlyなstatusへの直接代入があり型チェックが失敗した。オブジェクト置換へ修正して成功を確認。テストの期待値を緩める対応はしていない。
- 実装コミット `4d0959338f94bf29503e6b8aa8a7a4307e301bac` の[CI run 35848465588](https://github.com/tapioca24/changemap/actions/runs/35848465588)で、Linux/macOS/Windows × Node.js 24.11.0/24.x全6ジョブの成功を2026-09-23に確認。各ジョブで型チェック・lint・整形・テスト（buildを含む）・1,000ノード配置検証・隔離配布検証を実行した。ブラウザ描画性能の測定はmacOSのみであり、3 OSの描画測定が済んだとは扱わない。
- npm公開とバージョン更新は実施しない。初版の英語README・MITライセンス・ビルド・隔離導入は揃っているが、公開はユーザーの別指示と対象commitのCI確認後。

## スキル・設計判断

ユーザー指定のgrill-with-docsに従いgrillingとdomain-modelingを読み、性能の未確定基準だけを確認した。新しい製品用語や不可逆な設計決定はなく、CONTEXT.md/ADRの追加は不要。React Flowのmemo化はfind-docsで [公式性能ガイド](https://reactflow.dev/learn/advanced-use/performance) を確認した。ブラウザ測定にはagent-browserを使用。
